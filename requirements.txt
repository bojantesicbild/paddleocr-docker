# Core OCR: Docling (IBM). Brings its own torch + transformers + layout/table
# models. No paddle on this branch.
docling>=2.20,<3

# PDF → image and image I/O. Docling can ingest PDFs directly, but we keep
# PyMuPDF to render per-page PNGs so the per-page output contract (region
# prefixes) matches the existing API.
PyMuPDF==1.27.1
pillow==12.1.0

# HTML table → GFM conversion (app/markdown_format.py)
markdownify>=0.11

# Post-OCR language detection
langdetect>=1.0.9,<2

# HTTP layer
fastapi>=0.110,<1
uvicorn[standard]>=0.27,<1
python-multipart>=0.0.9
pydantic>=2.7,<3
jinja2>=3.1,<4

# Queue
redis>=5.0,<6
rq>=1.16,<2

# BeautifulSoup (for any HTML table cleanup left in markdown_format.py)
beautifulsoup4>=4.12
